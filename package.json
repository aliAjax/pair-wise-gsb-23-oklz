{
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5109",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 5109",
    "typecheck": "tsc --noEmit",
    "check:rules": "esbuild scripts/check-rules.ts --bundle --platform=node --format=esm --outfile=scripts/.check-rules.mjs --log-level=warning && node scripts/.check-rules.mjs; status=$?; rm -f scripts/.check-rules.mjs; exit $status"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "typescript": "^5.8.3",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0"
  }
}
